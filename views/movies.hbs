<h2>Ironhack Cinema</h2>
<p>Click on the movie to check the showtimes</p>

<div class="movieview">
  {{#each movieInfos}}
    <div class="dispmovies">
      <img src="{{this.image}}" alt="" class="movimage" />
      <h3 class="title">{{this.title}}</h3>
      <a href="/movies/{{this.id}}" class="btn">See more</a>
    </div>
  {{/each}}
</div> 